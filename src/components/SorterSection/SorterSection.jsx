import React from 'react'
import "./SorterSection.css"

const SorterSection = () => {
    return (
        <div className="sorter-background">
            <button>Sort by completed</button>
            <button>Sort by Date</button>
        </div>
    )
}

export default SorterSection
